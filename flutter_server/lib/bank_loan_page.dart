import 'package:flutter/material.dart';

class BankLoanPage extends StatelessWidget {
  const BankLoanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bank Loan Service')),
      body: const Center(child: Text('Welcome to Bank Loan Service')),
    );
  }
}
